import { cards } from '../../src/lib/game/cards';
const card = (id: string) => { const c = cards.find(c => c.id === id); if (!c) throw Error(id); return c; };
const face = (id: string, caption = '', selected = false) => `<figure class="face ${['Leader','Event'].includes(card(id).type) ? 'landscape' : ''} ${selected ? 'selected' : ''}"><img src="/docs/ux/cards/${id}.webp" alt="${card(id).name}">${caption ? `<figcaption>${caption}</figcaption>` : ''}</figure>`;
const faces = (ids: string[]) => `<div class="faces">${ids.map(id => face(id)).join('')}</div>`;
const icon = (id: string, value = '') => `<span class="icon ${id}"><img src="/static/assets/${id === 'cards' ? 'backs/back-deck' : `icons/${id === 'discard' ? 'discard-v2' : id}`}.webp" alt="${id}"><b>${value}</b></span>`;
const button = (label: string, primary = false, disabled = false) => `<button ${disabled ? 'disabled' : ''} class="${primary ? 'primary' : ''}">${label}</button>`;
const row = (...items: string[]) => `<div class="row">${items.join('')}</div>`;
const note = (text: string) => `<p class="note">${text}</p>`;
const panel = (title: string, body: string) => `<section class="panel"><h2>${title}</h2>${body}</section>`;
const badge = (s: string) => `<span class="badge">${s}</span>`;
const backs = `<div class="backs">${[1,2,3,4,5].map(() => '<img src="/static/assets/backs/back-deck.webp" alt="Hidden card">').join('')}</div>`;
const counters = (a = 1, c = 0, b = 1, w = 1) => `<div class="counters">${[['actions',a],['coins',c],['buys',b],['worship',w]].map(([k,v]) => `<div>${icon(String(k),String(v))}<span>${k}</span></div>`).join('')}</div>`;
const activity = `<ol class="activity"><li><b>Theseus bought Polis</b><span>Supply → Theseus’s discard · spent 5 Coins, 1 Buy</span></li><li><b>Theseus played Drachma</b><span>Hand → play · +2 Coins</span></li><li><b>Theseus played Sea Trade</b><span>Hand → play · +2 Coins, +1 Buy</span></li></ol>`;
const hand = `<div class="faces hand">${face('temple-of-athena','✓ Temple · ready to play',true)}${face('obol','Treasure phase')}${face('hamlet','Scores at game end')}</div>${note('Cards 1–3 of 5 · 2 more Obols')}${row(button('Previous',false,true),button('Next cards'))}`;

const leader = `${face('thaleia')}${note('Bloodline ready · first Athena Action → +1 Action. Leaders grant no Devotion.')}`;
const event = `${face('counsel-of-olympus')}${note('Shared with everyone · Athena Devotion 2 · Temple of Athena + Oracle’s Acolyte in play. Favored replaces Standard')}`;
const market = `<div class="market">${['obol','drachma','talent','hamlet','polis','acropolis'].map(id => `<div>${face(id)}${row(badge(`${id === 'obol' ? 40 : id === 'drachma' ? 30 : id === 'talent' ? 20 : 6} left`),button(`Inspect`))}</div>`).join('')}</div>`;
const fields = `<label>Your name<input value="Ariadne" maxlength="24"></label><label>Players</label>${row(button('2 players',true),button('3'),button('4'))}`;

export const screens = [
  {id:'01-entry',title:'Take a seat',section:'Before play',story:'002',body: panel('Play with friends', `${faces(['thaleia','nereon'])}${note('Your seat is remembered in this browser. No account form is needed.')}${fields}${row(button('Create table',true),button('Open invitation'))}${note('Already playing here? Resume your table from the saved-table list.')}`), action:'Enter a name, select 2–4 seats and create; follow an invitation to join; resume an existing seat. Anonymous sign-in happens before mutation.',signifier:'Selected player count has a gold outline and text. Create is disabled while signing in; inline validation names the field.',transition:'Create → 03; invitation → 02; resume → latest legal state. Sign-in failure → 23.'},
  {id:'02-invitation',title:'Join Ariadne’s table',section:'Before play',story:'002',body: '',action:'Review host and capacity, enter your name, then Join table once. An existing member resumes instead of taking a second seat.',signifier:'Capacity is explicit; joining shows one pending state and disables resubmission.',transition:'Acknowledged join → 03. Full, missing or started table → 23. The last-seat race has one winner.'},
  {id:'03-lobby',title:'Your table',section:'Before play',story:'002',body: `<div class="columns">${panel('Seats · 2 of 2',`${row(badge('Ariadne · host · here'),badge('Theseus · here'))}${note('Turn order is drawn when the host starts. Leaders are chosen in reverse turn order.')}${row(button('Copy invitation'),button('Begin leader draft',true))}${note('Theseus joined the table.')} ${backs}`)}${panel('What you will start with',`${faces(['obol','hamlet','temple-of-athena'])}${note('6 Obols + 3 Hamlets + your god’s Temple. The Temple shown is an example until you choose a leader.')}`)}</div>`,action:'Copy the invitation. Host starts once every seat is occupied; guests see “Waiting for Ariadne to start.”',signifier:'Each occupied seat names its player and connection state. Start has a visible reason when disabled.',transition:'Join animates seat entrance once. Start commits first player and draft order → 04; no cards are dealt yet.'},
  {id:'04-draft',title:'Choose your bloodline',section:'Leader draft',story:'003',body:`${note('Turn order: Ariadne → Theseus. Theseus chooses first; Ariadne chooses last.')}${row(badge('Theseus is choosing'),badge('Your choice is next'))}<div class="draft">${['thaleia','nereon','melia','doreios'].map(id=>face(id)).join('')}</div>${row(button('Inspect Thaleia'),button('Choose Thaleia',false,true))}`,action:'Active drafter inspects an unused leader and confirms Choose [name]. Other players browse but cannot claim.',signifier:'Current drafter is named; chosen leaders show owner and are unavailable. Landscape leaders retain their larger form factor.',transition:'Claim travels to its seat; matching event and Temple preview appear. Advance backward in turn order. Last claim → 05.'},
  {id:'05-ready',title:'Your empire begins',section:'Deal',story:'004',body:`<div class="columns">${panel('Ariadne · Athena',`${face('thaleia')}${note('You take the first turn. Thaleia’s first matching Action grants +1 Action.')}`)}${panel('Your starting deck',`${faces(['obol','hamlet','temple-of-athena'])}${note('6 Obols · 3 Hamlets · 1 Temple of Athena. Shuffle all ten and draw five.')}${row(button('See my hand',true))}`)}</div>${note('Shared gods: Athena and Poseidon. All twelve Action piles are available. Territories: 6 each · Actions: 8 each.')}`,action:'See my hand dismisses the setup explanation locally; no readiness vote or extra game rule is introduced.',signifier:'Your actual leader, god and Temple are shown together. The first player and supply scale are explicit.',transition:'Authoritative setup commits once after the last draft. Deal five backs into each seat; only the owner sees faces → 06.'},
  {id:'06-actions',title:'Your turn · Actions',section:'At the table',story:'005',body:`${counters()}<div class="columns">${panel('Your hand · 5 cards',`${hand}${row(button('Play Temple',true),button('Go to Treasures'))}`)}${panel('Your bloodline',leader)}</div>${note('Deck 5 · discard 0 · in play 0. Select a card to inspect before playing. Worship is available between effects.')}`,action:'Select a hand card; Play spends an Action and resolves all effects plus its leader trigger. Inspect is always separate from committing. Advance to Treasures voluntarily.',signifier:'Legal cards have a gold outline and “Ready to play”; selected cards have a checkmark. Territories explain that they score at game end.',transition:'Hand → play motion and counter changes, then leader pulse. Choices → 12/13/14. Phase advance → 07. Worship → 10.'},
  {id:'07-treasures',title:'Your turn · Treasures',section:'At the table',story:'006',body:`${counters(2,0,1,2)}${panel('Choose Treasures to play',`${faces(['obol','obol','obol','hamlet'])}${row(button('Play selected Obol',true),button('Play all Treasures'),button('Go to Buys'))}${note('Play one at a time to keep a Treasure in hand for a later effect. You cannot return to Actions.')}`)}${note('In play: Temple of Athena · Athena Devotion 1 · Bloodline used')}`,action:'Play individual Treasures or use explicit Play all Treasures. Continue to Buys; confirmation is required only if playable Treasures would be left behind.',signifier:'Coins update as each committed Treasure enters play. Hand cards remain targetable for later trash effects if left unplayed.',transition:'Each played card moves to the play area; batch playback remains ordered. Between individual plays Worship remains available → 10. Advance → 08.'},
  {id:'08-supply',title:'Your turn · Buys',section:'At the table',story:'007',body:`${counters(0,5,1,1)}${row(badge('Basics · 6 piles'),button('Actions · 12 piles'),button('Filter by god'))}${market}${note('All 18 supply piles remain accessible. Empty piles: 0 of 3 · Acropolis: 6 left.')}`,action:'Switch Basics/Actions, filter only the view, inspect a pile, and confirm Buy in its detail panel. Buy a cost-0 card still spends one Buy.',signifier:'Each pile has a separate remaining count, independent of the face’s physical N/M. Affordable piles gain a Buy affordance; others state “Need 3 more Coins”, “No Buys” or “Empty”.',transition:'Pile selection → 09. Purchase moves one face to discard; count and counters settle atomically. Additional buys stay here. End turn → 17.'},
  {id:'09-inspect',title:'Polis',section:'Card details',story:'007',body:`<div class="columns inspector">${panel('Supply · 6 left',face('polis'))}${panel('Territory · Hestia',`${icon('victory','3')}${note('Costs 5 Coins and 1 Buy. Gain this card to your discard pile. It scores 3 VP at game end.')}${row(button('Buy Polis',true),button('Close'))}${note('After buying: 0 Coins · 0 Buys. This is a purchase, not Worship.')}`)}</div>`,action:'Read the full rules and inspect the unchanged card; confirm one context-specific action or close without a mutation. In-hand details offer Play only when legal.',signifier:'Destination, payment and resulting counters appear beside the committing button. Copy serial stays separate from supply stock.',transition:'Desktop modal, mobile full-height sheet; focus is trapped and restored on Close/Escape. Buy → 08, keeping the selected pile in view.'},
  {id:'10-worship',title:'Worship',section:'Shared gods',story:'008',body:`${counters(0,5,1,2)}<div class="columns">${panel('Athena · Favored',event + note('Pay 1 Worship + 3 Coins; no Buy.') + button('Worship Athena',true))}${panel('Poseidon · Standard',`${face('tribute-of-the-tides')}${note('Poseidon Devotion 0 · You may Worship another bloodline’s god.')}`)}</div>${row(button('Back to turn'))}${note('Pay 1 Worship + 3 Coins. No Buy is spent. Available between fully resolved effects, before cleanup.')}`,action:'Inspect any god selected at setup. Review automatically selected Standard/Favored effect and confirm Worship; no toggle can override Devotion.',signifier:'Matching in-play Actions are named as the Devotion source. Favored is labeled, not color alone; both printed effects remain visible.',transition:'Payment commits before resolution. Event stays in the shared area and never depletes. Resolve choices → 12/14; return to the original phase. Repeated Worship is legal if affordable.'},
  {id:'11-resolving',title:'Resolving Temple of Athena',section:'Effect resolution',story:'005',body:`${counters(1,0,1,2)}<div class="columns">${panel('Played by Ariadne',`${face('temple-of-athena')}${row(icon('worship','+1'),icon('actions','+1'))}`)}${panel('Next · Thaleia’s bloodline',`${face('thaleia')}${note('First Athena Action this turn → +1 Action. Resolves after the Temple.')}${button('Worship · finish effect first',false,true)}`)}</div>`,action:'Wait for automatic instructions; make a choice only when the effect requests it. Do not allow another command to interrupt a card or leader.',signifier:'Source card, instruction and upcoming leader are visible. Disabled actions name the reason; progress is finite, not an indefinite spinner.',transition:'Resolve printed instructions in order, then leader. This is a transient state; assert it using a controlled animation clock, not a timing race. Settle → prior phase.'},
  {id:'12-trash',title:'Seed Keeper · choose cards',section:'Effect choice',story:'009',body:`${row(icon('trash','2'),badge('1 selected · up to 2'))}${panel('From your hand',`<div class="faces">${face('hamlet','✓ Selected · cost 2',true)}${face('obol','Cost 0')}${face('drachma','Cost 3')}</div>${note('Trashed cards leave your deck and score for nobody. Cards already in play cannot be selected.')}`)}${row(button('Trash 1 card',true),button('Trash none'))}`,action:'Toggle zero to the permitted maximum from your hand; confirm exact selected count. Trash none is always available for optional trash, including Doreios.',signifier:'Selected cards have a checkmark and a running count. The red-tinted destination uses the approved broken-card icon and text.',transition:'Confirmed cards reveal publicly and move to shared trash. Continue the effect; for Forge/Trial zero means no gain. Doreios consumes the trigger even when skipped.'},
  {id:'13-discard',title:'Harvest Feast · discard 1',section:'Effect choice',story:'009',body:`${row(icon('discard','1'),badge('1 selected · required'))}${panel('Choose from your hand after drawing',`<div class="faces">${face('hamlet','✓ Selected',true)}${face('obol','In hand')}${face('council-of-sages','Just drawn')}</div>${note('Draw two first. You may discard a card you just drew. Discarded cards stay yours.')}`)}${row(button('Discard Hamlet',true),button('Continue',false,true))}`,action:'Choose the required number from the post-draw hand. No skip while an eligible card exists; do as much as possible if the hand is smaller.',signifier:'Required count and destination distinguish this from optional trash and topdeck. Newly drawn cards are labeled without excluding them.',transition:'Hand → own discard, then the leader trigger and prior phase. An empty hand auto-completes with a named “No cards to discard” message.'},
  {id:'14-gain',title:'Counsel of Olympus · gain',section:'Effect choice',story:'010',body:`${row(icon('gain','5'),icon('topdeck'),badge('Action · Favored'))}${note('Choose one Action costing up to 5. Place it on top of your deck; it is not drawn or played.')}${panel('Eligible supply · page 1 of 4',`<div class="faces">${face('oracles-acolyte')}${face('council-of-sages')}${face('sacred-academy','✓ Selected',true)}</div>${row(button('Previous',false,true),button('Next choices'))}`)}${row(button('Gain Sacred Academy',true),button('Inspect card'))}`,action:'Select one eligible nonempty supply pile, inspect if needed, and confirm Gain. No Coins or Buys are charged by the choice itself. Optional Demeter gain offers Gain nothing.',signifier:'One gain icon contains the cost limit; topdeck appears only when required. Source effect, type restriction and destination are explicit; no ≤ or redundant Coin icon.',transition:'Reveal the gained face publicly, decrement stock and move to discard or topdeck. No eligible pile auto-skips gain, explains why, and still resolves later instructions.'},
  {id:'15-reveal',title:'Victorious Procession',section:'Effect resolution',story:'011',body:`<div class="columns">${panel('Revealed from your deck',`${face('hamlet')}${row(icon('discard'),icon('coins','+2'))}`)}${panel('A Territory was revealed',`${note('Hamlet moves to your discard pile. Gain 2 more Coins. The initial +2 Coins and +1 Buy have already resolved.')}${note('Everyone sees the revealed face. It is held outside the deck and discard until this instruction finishes.')}${button('Resolving…',false,true)}`)}</div>`,action:'No choice is invented for a conditional reveal. Resolve automatically; open the activity record afterward to inspect what happened.',signifier:'Show the revealed face and the exact branch/destination. A non-Territory shows Topdeck instead, without the bonus Coins.',transition:'Deck back → public face → discard or deck. If deck empty, shuffle discard first; if both empty, report no reveal but keep initial rewards.'},
  {id:'16-order',title:'Choose the next card drawn',section:'Effect choice',story:'011',body:`${row(icon('topdeck'),badge('First drawn → last drawn'))}${panel('Cards going onto your deck',`<div class="faces">${face('oracles-acolyte','1 · Draw first',true)}${face('harbor-pilot','2 · Draw second')}</div>${row(button('Move selected later'),button('Confirm order',true))}${note('Use Move earlier / later or the keyboard. Dragging is optional.')}`)}`,action:'When a future effect puts multiple cards on top together, set first-to-last draw order and confirm. This generic rules state is not reachable with the current card set.',signifier:'Numbered order and a “Draw first” label avoid ambiguous stack direction; move buttons are equivalent to drag.',transition:'Commit one order and return to the effect. Keep this screen dormant in v0.1; do not invent a card effect to expose it.'},
  {id:'17-end-turn',title:'Finish your turn?',section:'Cleanup',story:'012',body:`${counters(0,2,1,1)}${panel('Before cleanup',`${faces(['hamlet','obol'])}${note('You still have 2 Coins, 1 Buy and 1 Worship. You can buy a Hamlet or an Obol; you cannot afford Worship.')}${note('Ending discards your hand and played cards, clears resources, and draws five. You cannot Worship once cleanup begins.')}${row(button('Keep playing'),button('End turn',true))}`)}`,action:'Confirm cleanup when useful resources or legal actions remain; otherwise the explicit End turn command can proceed directly. Decline returns unchanged.',signifier:'Name actual remaining legal options, not just nonzero counters. No automatic end when Buys run out: Worship may still be possible.',transition:'Hand/play → discard, counters → zero, shuffle only as needed, draw five; check end condition after cleanup → 18 or 21.'},
  {id:'18-opponent',title:'Theseus’s turn · Buys',section:'At the table',story:'013',body:`${counters(0,0,1,1)}<div class="columns">${panel('Theseus bought Polis',`${face('polis')}${note('Supply → Theseus’s discard. Spent 5 Coins and 1 Buy. Their remaining counters are shown above.')}${backs}`)}${panel('Your hand is private',`${hand}${button('Inspect your deck count · 5')}`)}</div>${note('You may inspect public zones and your own hand. Only Theseus can act on this turn.')}`,action:'Follow the active player’s public moves; inspect your own hand and public piles while waiting. Do not queue out-of-turn game commands.',signifier:'Active player’s name stays next to resource counters. A persistent actor/card/destination sentence remains after animation.',transition:'Committed public actions animate once in event order. Private draws use backs. If another tab is open, show an activity badge without forcing navigation.'},
  {id:'19-zones',title:'Public piles',section:'Table reference',story:'014',body:`${row(button('Theseus’s discard',true),button('Shared trash'),button('In play'))}${panel('Theseus’s discard · 3 cards',`${faces(['polis','drachma','sea-trade'])}${note('All discarded cards are public. Inspect without changing order or contents.')}`)}${row(icon('cards','7'),badge('Theseus’s deck · count only'))}${note('No face, serial number or ordered list from another player’s deck or hand is available.')}`,action:'Switch public owner/zone, page through all cards and inspect faces. Deck inspection returns only the count; your own hand remains privately accessible.',signifier:'Zone heading includes owner and count; shared trash has a distinct broken-card icon. No reorder handles on read-only piles.',transition:'Open as desktop side panel or mobile full-height sheet. Close restores prior phase/tab/scroll/focus; state continues updating behind it.'},
  {id:'20-activity',title:'What happened',section:'Table reference',story:'013',body:`${panel('Latest public actions',activity)}${panel('Earlier this turn',`${face('nereon')}${note('Nereon’s bloodline: +1 Coin after Theseus’s first Poseidon Action. One matching Action is in play; Poseidon Devotion is 1.')}`)}${row(button('Earlier actions'),button('Return to table',true))}`,action:'Read public events in order, inspect referenced cards and load earlier history. No rewind, undo or replay-as-command action.',signifier:'Names, source → destination and resource deltas explain each event. Turn grouping replaces unstable wall-clock timestamps.',transition:'Append new activity with a small entrance; preserve the reader’s position and offer “New actions”. Reconnect catches up without replaying the entire animation queue.'},
  {id:'21-results',title:'Ariadne’s victory',section:'Game over',story:'015',body:`${note('The Acropolis pile emptied. The current turn finished; there is no final round.')}<div class="columns">${panel('Ariadne · 24 VP',`${row(icon('victory','24'),badge('12 turns'))}${faces(['hamlet','polis','acropolis'])}${note('3 Hamlets × 1 + 3 Poleis × 3 + 2 Acropoleis × 6 = 24')}`)}${panel('Theseus · 21 VP',`${row(icon('victory','21'),badge('11 turns'))}${note('3 Hamlets × 1 + 2 Poleis × 3 + 2 Acropoleis × 6 = 21')}${button('View scoring breakdown')}`)}</div>${row(button('Create another table',true),button('Review activity'))}`,action:'Review final territory totals across all owned zones and turns taken. Create another table returns to setup with the name remembered; it never resets this game.',signifier:'State the end reason, exact VP arithmetic and tie resolution. For equal points use fewer turns, then “Shared victory”.',transition:'After cleanup/check, final counters settle and results appear with a modest laurel fade. The completed table stays read-only and reloadable.'},
  {id:'22-connection',title:'Reconnecting to your table',section:'Recovery',story:'016',body:`${panel('Your seat is saved in this browser',`${backs}${note('You are offline. The table below is the last confirmed state. Game actions are unavailable until we reconnect.')}${row(button('Retry connection',true),button('Play card',false,true))}`)}${panel('Last confirmed · Ariadne’s Actions',`${counters()}${note('If a move was already sent, we will check whether it succeeded before you can try again.')}`)}`,action:'Retry connection; continue reading confirmed public information. Never optimistically queue purchases or privately reshuffle offline.',signifier:'Text + connection indicator distinguishes connecting, sending, synced, offline and failed; “Sending…” is not success.',transition:'On recovery restore canonical state/private view and reconcile command ID. No duplicate effect, no animation backlog. Restore pending choice or show whose turn it now is.'},
  {id:'23-unavailable',title:'This table is full',section:'Recovery',story:'016',body:`${panel('All seats have been taken',`${face('thaleia')}${note('Another player joined the final seat first. You have not joined this game.')}${row(button('Create your own table',true),button('Try another invitation'))}${note('If this was your seat, return using the original browser. An anonymous seat cannot be recovered after its browser storage is cleared.')}`)}`,action:'Recover using the original browser or return to setup. Missing/invalid invitation, already-started nonmember, sign-in failure and unsupported history use this same shell with specific copy and safe actions.',signifier:'Plain-language reason; no raw Firebase error, game document ID, stack trace or disabled spinner trap.',transition:'Retry only recoverable failures. Lost identity never silently replaces another seat. Existing members may resume a full or started game.'},
  {id:'24-table-display',title:'Shared table',section:'Tabletop',story:'017',body:`${note('PUBLIC DISPLAY · Private hands stay on each player’s own device.')}<div class="columns">${panel('Ariadne · Athena · Actions',`${face('thaleia')}${counters()}${backs}${note('Hand 5 · deck 5 · discard 0')}`)}${panel('Theseus · Poseidon',`${face('nereon')}${backs}${note('Hand 5 · deck 5 · discard 0')}`)}</div>${row(button('Supply'),button('Shared gods'),button('Activity'),button('Connect a hand'))}${note('Display controls inspect only. Turns are played from the private controllers.')}`,action:'Open a read-only public-display session using a short-lived host-authorized display link; inspect supply, gods and activity. Each seated player uses their existing browser/controller.',signifier:'PUBLIC DISPLAY label is persistent. Only deck backs/counts appear for hands; no “show hand” button exists on this surface.',transition:'Same committed event stream, public projection only. Rotation/4K layout changes presentation, never turn order. A display capability cannot read private documents or submit player commands.'},
  {id:'25-controller',title:'Your private controller',section:'Tabletop',story:'017',body:`${note('Ariadne · Athena · Connected to shared table')}${counters()}${panel('Your hand · only on this device',`${hand}${row(button('Play Temple',true),button('Go to Treasures'))}`)}${row(button('Supply'),button('Worship'),button('Public activity'))}`,action:'Use all normal turn/choice controls from a private seat session. Open display link separately; do not move the anonymous identity to another device through a room link.',signifier:'Seat name and whose turn it is remain visible. Controller is the normal private gameplay view, not a second participant.',transition:'Controller command acknowledges → public display receives public movement. Private new cards only reach the owner. Disconnect follows 22.'},
  {id:'26-table-options',title:'Table options',section:'Tabletop',story:'017',body:`${panel('Display and access',`${note('Ariadne · host. Your seat lives in this browser; an invitation does not transfer it.')}${row(button('Copy invitation'),button('Create public display link',true))}${note('Open the display link on the shared screen. It grants public viewing only; it cannot join a seat or play cards.')}${row(button('Revoke display link'),button('Return to game'))}`)}${panel('Motion and help',`${row(button('Follow device motion setting',true),button('Reduce motion'))}${note('Reduced motion retains the same activity messages and final state.')}${row(button('Public piles'),button('Activity'),button('Rules'),button('Catalog'))}`)}`,action:'Copy lobby invite, issue/revoke a public-display capability as host, or set reduced motion locally. No restart, kick, surrender, transfer-seat or speed setting is implied for v0.1.',signifier:'Player invite and public display link are explicitly different controls. Copy provides textual success/failure; a selectable URL is the fallback.',transition:'Options overlay returns to prior view. Leaving the page keeps the seat; untrusted display links expire/revoke and return to 23. Capability support is future backend work.'},
  {id:'27-rules',title:'How to play',section:'Reference',story:'018',body:`${row(button('Setup'),button('Cards'),button('Leaders'),button('Your turn',true),button('Gods'),button('Winning'),button('Reminders'))}${panel('Your turn',`${note('Start with 1 Action, 1 Buy, 1 Worship and 0 Coins. Play Actions, then Treasures, then buy cards. You may Worship between resolved effects in any phase before cleanup.')}${face('temple-of-athena')}${note('Your Temple adds 1 Worship and 1 Action. Finish its effect and your leader trigger before Worship. Cleanup discards hand and play, clears resources and draws five.')}`)}${button('Return to game',true)}`,action:'Open contextual rules, use the shared sticky section navigation, read actual card examples and return without losing the pending game choice.',signifier:'Concise document prose and familiar card imagery; current section has an underline and aria-current. A game badge indicates ongoing activity.',transition:'Rules scroll as a document; they do not pause the game. Return restores the prior view, unless acknowledged state has advanced. Existing /rules remains the canonical summary.'},
  {id:'28-catalog',title:'Card reference',section:'Reference',story:'018',body:`${row(button('Leaders'),button('Events'),button('Treasures'),button('Territories'),button('Actions',true),button('All cards'))}<label>Search cards<input placeholder="Name or effect"></label>${panel('Actions · Athena',`${faces(['oracles-acolyte','council-of-sages','sacred-academy'])}${note('Inspect any card to read its full text. The catalog shows inventory copy numbers, not your game’s remaining piles.')}${row(button('All gods'),button('Show backs'),button('Print cards'))}`)}${button('Return to game',true)}`,action:'Search/filter all thirty definitions, inspect any physical copy/front/back and print using the established gallery. No catalog button purchases a card.',signifier:'Same StickyNav as the rules; clear “Card reference” scope. Deck/event/leader backs remain distinct.',transition:'Reference opens without replacing the seat. The full existing gallery remains a scrolling document; return restores the game context.'}
];
// Invitation uses the approved common back; it has no catalog definition of its own.
screens[1].body = panel('A seat is waiting', `${backs}${note('Host: Ariadne · 1 of 2 seats occupied')}<label>Your name<input value="Theseus"></label>${row(button('Join table',true),button('Back'))}${note('Joining keeps your seat in this browser. You can reload and return.')}`);

const css = `
@font-face{font-family:Atkinson;src:url('/node_modules/@fontsource/atkinson-hyperlegible/files/atkinson-hyperlegible-latin-400-normal.woff2')}@font-face{font-family:Cormorant;src:url('/node_modules/@fontsource/cormorant-garamond/files/cormorant-garamond-latin-600-normal.woff2');font-weight:600}
*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden}body{font-family:Atkinson,sans-serif;background:#151b1b;color:#ede9de;font-size:17px}button,input{font:inherit}button{min-height:44px;padding:10px 17px;border:1px solid #92805b;background:#26332f;color:#f1e6ce;border-radius:5px;cursor:pointer}button.primary{background:#dfc48d;color:#17241f;border-color:#dfc48d}button:disabled{opacity:.5;cursor:default}input{display:block;width:100%;padding:12px;margin:8px 0 18px;background:#101817;color:#fff;border:1px solid #8e896f;border-radius:5px}label{display:block;margin-top:12px}.app{height:100dvh;display:grid;grid-template-rows:70px 1fr 68px}header{padding:12px 32px;border-bottom:1px solid #766849;display:flex;justify-content:space-between;align-items:center;background:#101817}.brand{font:600 27px/1 Cormorant,serif;letter-spacing:3px}.brand small{display:block;font:10px Atkinson;letter-spacing:6px;color:#dbc08c;margin-top:5px}.status{font-size:14px;color:#cfdbcc}main{min-height:0;padding:25px 36px;display:flex;flex-direction:column;gap:15px;max-width:1600px;width:100%;margin:auto;height:100%}.eyebrow{font-size:12px;letter-spacing:2px;text-transform:uppercase;color:#d6ba7f;margin:0}h1{font:600 42px/1.05 Cormorant,serif;margin:0}h2{font:600 28px/1.1 Cormorant,serif;margin:0 0 12px;color:#e9d6b0}.content{overflow:auto;min-height:0;flex:1;padding:2px 8px 12px 2px;scrollbar-color:#aa976d #1a2925;scrollbar-width:thin;display:flex;flex-direction:column;gap:18px}.columns{display:grid;grid-template-columns:1.2fr 1fr;gap:22px;align-items:start}.panel{padding:20px;border:1px solid #726546;border-radius:10px;background:linear-gradient(140deg,#263630,#1c2725);min-width:0}.panel>.face{max-width:205px;margin:0 auto}.panel>.face.landscape{max-width:430px}.faces{display:flex;gap:12px;align-items:flex-start;justify-content:center}.face{margin:0;min-width:0;flex:1;max-width:175px;padding:3px;border:2px solid transparent;border-radius:9px}.face.landscape{max-width:340px}.face img{display:block;width:100%;height:auto}.face.selected{border-color:#e5c777;background:#e2c77615}.face figcaption{font-size:14px;margin-top:6px;text-align:center;line-height:1.25}.row{display:flex;align-items:center;gap:10px;flex-wrap:wrap}.row+.row{margin-top:8px}.note{font-size:16px;line-height:1.45;color:#c9cfc0;margin:12px 0}.badge{font-size:14px;line-height:1.4;padding:6px 10px;background:#101c19;border:1px solid #5f705e;border-radius:5px}.counters{display:flex;gap:20px;align-items:center;padding:8px 18px;background:#111e1a;border:1px solid #6b6548;border-radius:8px;align-self:start}.counters>div{display:flex;align-items:center;gap:3px}.counters span:not(.icon){text-transform:capitalize;font-size:14px}.icon{position:relative;display:inline-block;width:64px;height:64px;flex-shrink:0}.icon img{width:100%;height:100%;object-fit:contain}.icon b{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);font-size:24px;color:#fff7dc;text-shadow:0 1px 3px #000,1px 1px #000}.icon.worship b,.icon.buys b{top:68%}.icon.gain b{left:55%;top:42%}.icon.victory b{top:44%}.icon.trash b,.icon.discard b{left:48%;top:45%}.icon.cards img{padding:0 9px}.backs{display:flex;gap:10px;justify-content:center;margin:16px 0}.backs img{width:65px;height:91px;border-radius:6px}.draft{display:grid;grid-template-columns:1fr 1fr;gap:18px;max-width:1050px;margin:auto}.draft .face{max-width:450px}.market{display:grid;grid-template-columns:repeat(6,1fr);gap:15px}.market .face{max-width:180px;margin:auto}.market .row{justify-content:center}.inspector .panel>.face{max-width:290px}.activity{padding-left:23px;margin:0}.activity li{padding:10px 0}.activity span{display:block;color:#bacabf;margin-top:5px}footer{border-top:1px solid #786949;display:flex;align-items:center;justify-content:space-between;padding:8px 32px;background:#101817;gap:16px}.tabs{display:flex;gap:22px}.tabs span{padding:12px 0;font-size:15px}.tabs .active{color:#e6c983;border-bottom:2px solid #e6c983}.design-label{font-size:11px;color:#a5b7a6;white-space:nowrap}.section-note{font-size:13px;color:#b7cbbf;margin-left:auto}
@media(max-width:700px){body{font-size:16px}.app{grid-template-rows:61px 1fr 60px}header{padding:10px 16px}.brand{font-size:22px}.brand small{font-size:8px}.status{font-size:12px}main{padding:17px 14px;gap:10px}h1{font-size:32px}h2{font-size:25px}.eyebrow{font-size:10px}.content{gap:14px;padding-right:5px}.columns{grid-template-columns:1fr;gap:14px}.panel{padding:14px}.panel>.face{max-width:160px}.panel>.face.landscape{max-width:310px}.faces{gap:5px}.face{padding:1px;border-radius:5px}.face figcaption{font-size:12px}.note{font-size:15px;line-height:1.35}.counters{gap:7px;padding:5px 8px;width:100%;justify-content:space-between}.counters>div{flex-direction:column;gap:0}.counters span:not(.icon){font-size:11px}.icon{width:50px;height:50px}.icon b{font-size:21px}.row{gap:7px}.row button{padding:10px 12px;font-size:14px}.badge{font-size:12px}.draft{grid-template-columns:1fr;gap:12px}.draft .face{max-width:100%}.market{grid-template-columns:repeat(2,1fr);gap:18px 10px}.market .row{gap:4px}.market .face{max-width:145px}.backs{gap:6px}.backs img{width:51px;height:71px}.inspector .panel>.face{max-width:190px}footer{padding:5px 14px;gap:4px}.tabs{gap:16px}.tabs span{font-size:13px}.design-label{font-size:9px;max-width:65px;white-space:normal;text-align:right}.section-note{display:none}}
`;
export function render(id: string) {
  const s = screens.find(s => s.id === id)!;
  if (!s) throw Error(`Unknown screen ${id}`);
  const n = Number(s.id.slice(0,2));
  const tabs = n < 6 ? ['Your table', 'Rules', 'Cards'] : n === 24 ? ['Table', 'Supply', 'Gods', 'Activity'] : ['Hand', 'Supply', 'Gods', 'More'];
  const active = [8,9,14].includes(n) ? 'Supply' : n === 10 ? 'Gods' : [19,20,26,27,28].includes(n) ? 'More' : tabs[0];
  return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${s.title} · Pantheon UX mockup</title><style>${css}</style><body><div class="app"><header><div class="brand">PANTHEON<small>BLOODLINES</small></div><div class="status">${s.id === '22-connection' ? '○ Offline' : s.id === '23-unavailable' ? 'Table unavailable' : '● Connected'} · ${n===2?'Theseus':n===24?'Public view':'Ariadne'}</div></header><main><p class="eyebrow">${s.section}</p><h1>${s.title}</h1><div class="content" data-scroll>${s.body}</div></main><footer><div class="tabs">${tabs.map(t=>`<span class="${t===active?'active':''}">${t}</span>`).join('')}</div><span class="section-note">${n===24?'Public display · no private hands':n<6?'Choose your table · learn the rules':'Private view · your hand is visible only to you'}</span><span class="design-label">DESIGN STUDY<br>${s.id}</span></footer></div></body></html>`;
}
